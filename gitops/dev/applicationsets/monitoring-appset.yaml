apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: monitoring-stack
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - list:
              elements:
                - env: dev
          - list:
              elements:
                - component: kube-prometheus-stack
                  chartRepo: https://prometheus-community.github.io/helm-charts
                  chartName: kube-prometheus-stack
                  chartVersion: "82.4.3"
                  valuesFile: values-stacks/dev/kube-prometheus-stack-values.yaml
                - component: loki
                  chartRepo: https://grafana.github.io/helm-charts  # NOTE: Loki는 grafana.github.io 유지 (community 미이관, 2026-03 확인)
                  chartName: loki
                  chartVersion: "6.53.0"
                  valuesFile: values-stacks/dev/loki-values.yaml
                - component: tempo
                  chartRepo: https://grafana-community.github.io/helm-charts
                  chartName: tempo
                  chartVersion: "1.24.4"
                  valuesFile: values-stacks/dev/tempo-values.yaml
                - component: mimir
                  chartRepo: https://grafana.github.io/helm-charts  # NOTE: Mimir는 grafana.github.io 유지
                  chartName: mimir-distributed
                  chartVersion: "6.0.5"
                  valuesFile: values-stacks/dev/mimir-values.yaml
                - component: pyroscope
                  chartRepo: https://grafana.github.io/helm-charts  # NOTE: Pyroscope는 grafana.github.io 유지
                  chartName: pyroscope
                  chartVersion: "1.18.1"
                  valuesFile: values-stacks/dev/pyroscope-values.yaml
                - component: otel-collector-front
                  chartRepo: https://open-telemetry.github.io/opentelemetry-helm-charts
                  chartName: opentelemetry-collector
                  chartVersion: "0.147.1"
                  valuesFile: values-stacks/dev/otel-collector-front-values.yaml
                - component: otel-collector-back
                  chartRepo: https://open-telemetry.github.io/opentelemetry-helm-charts
                  chartName: opentelemetry-collector
                  chartVersion: "0.147.1"
                  valuesFile: values-stacks/dev/otel-collector-back-values.yaml
  template:
    metadata:
      name: "{{component}}-{{env}}"
      annotations:
        argocd.argoproj.io/compare-options: ServerSideDiff=true
    spec:
      project: monitoring
      sources:
        - repoURL: "{{chartRepo}}"
          chart: "{{chartName}}"
          targetRevision: "{{chartVersion}}"
          helm:
            valueFiles:
              - "$values/{{valuesFile}}"
        - repoURL: "https://github.com/Team-Ikujo/Goti-monitoring.git"
          targetRevision: main  # dev only: HEAD tracking. prod MUST pin to tag/SHA
          ref: values
      destination:
        server: "https://kubernetes.default.svc"
        namespace: monitoring
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        retry:
          limit: 5
          backoff:
            duration: 10s
            factor: 2
            maxDuration: 3m
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
      ignoreDifferences:
        - group: admissionregistration.k8s.io
          kind: MutatingWebhookConfiguration
          jsonPointers:
            - /webhooks/0/clientConfig/caBundle
        - group: admissionregistration.k8s.io
          kind: ValidatingWebhookConfiguration
          jsonPointers:
            - /webhooks/0/clientConfig/caBundle
