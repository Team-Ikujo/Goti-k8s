nameOverride: goti-guardrail

replicaCount: 0

image:
  repository: "707925622666.dkr.ecr.ap-northeast-2.amazonaws.com/goti-guardrail"
  tag: "dev-1-b7eaff7"
  pullPolicy: Always

imagePullSecrets:
  - name: ecr-creds

podAnnotations:
  # dev only: 호스트 PC Redis는 Envoy 프록시 우회
  traffic.sidecar.istio.io/excludeOutboundPorts: "6379"

service:
  type: ClusterIP
  port: 8000
  name: http

probes:
  liveness:
    httpGet:
      path: /health
      port: http
    initialDelaySeconds: 5
    periodSeconds: 10
    failureThreshold: 3
  readiness:
    httpGet:
      path: /health/ready
      port: http
    initialDelaySeconds: 3
    periodSeconds: 5
    failureThreshold: 3

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi

env:
  - name: GUARDRAIL_REDIS_URL
    value: "redis://172.20.0.1:6379/1"  # dev only: 호스트 PC Redis (Kind bridge GW), DB 1번. prod에서는 ExternalSecret으로 교체

gateway:
  enabled: false  # Guardrail은 Istio ExtAuthz로 연동 (VirtualService 불필요)

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65534
  fsGroup: 65534

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: ["ALL"]
  seccompProfile:
    type: RuntimeDefault

configMap:
  enabled: false

externalSecret:
  enabled: false  # dev only: Redis 인증 미사용. prod에서는 enabled: true + remoteRef 설정

# --- Istio 보안/네트워크 정책 ---
istioPolicy:
  authorizationPolicy:
    enabled: false  # ExtAuthz 서버로 동작하여 자체 AuthorizationPolicy 불필요. 호출 제한은 Istio mesh 레벨에서 처리
  destinationRule:
    enabled: true
  sidecar:
    enabled: true
    egress:
      - hosts:
          - "istio-system/*"
          - "./*"
          - "monitoring/*"
